/*
const autoCompleteConfig = [{
    name: 'Datos',
    debounceMS: 250,
    minLength: 2,
    maxResults: 10,
    inputSource: document.getElementById('txtBuscarProd'),
    targetID: document.getElementById('inputID1'),
    fetchURL: 'autocomp_producto_pista.php?term={term}',
    fetchMap: {id: "id",
               name: "descripcion"}
  }
];
//console.log(autoCompleteConfig);
// Initiate Autocomplete to Create Listeners
autocompleteBS(autoCompleteConfig);
function resultHandlerBS(inputName, selectedData) {
  alert(selectedData.id_producto)
  $("#modalCombustibles .modal-body  #idProdSelected").val(selectedData.id_producto);
  console.log(selectedData.descripcion);
}
*/
